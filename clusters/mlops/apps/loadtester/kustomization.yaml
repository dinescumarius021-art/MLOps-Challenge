apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  - oauth2-proxy.yaml
  - oauth2-proxy-ingress.yaml

labels:
  - pairs:
      environment: dev
      challenge: raiffeisen-mlops
    includeTemplates: true